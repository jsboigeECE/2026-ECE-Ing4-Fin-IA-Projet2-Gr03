# Notebooks

This directory can contain Jupyter notebooks for interactive exploration.

To run the interactive analysis:

```bash
# Install Jupyter
pip install jupyter

# Launch
jupyter notebook notebooks/
```

Note: The primary analysis is in `src/` and can be run via the CLI pipeline.
See `python -m src.pipeline.run_pipeline --help` for details.
